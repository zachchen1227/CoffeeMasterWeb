import { updateCartCount } from "./db.js";

document.addEventListener('DOMContentLoaded', initializePage);

function initializePage(){

    // document.getElementsById("cart-button").addEventListener("click",function(){
    //     alert("已送出訂單")
    //     document.getElementsByClassName("cmw-count")[0].innerText=0;
    // })
}
